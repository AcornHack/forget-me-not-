<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.forgetmenot.forgetmenot">

    <application
        android:allowBackup="true"
        android:icon="@drawable/forgetlogo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MemoActivity"
            android:label="@string/title_activity_memo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MemoViewActivity"
            android:label="@string/title_activity_memo_view"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <receiver android:name=".MemoNotificationReceiver" />
    </application>

</manifest>
